# FIDO Conformance: CTAP2.3 (PASS)

**180 passed · 0 failed · 714.56 s.**
Run 2026-09-25 with the FIDO Alliance Conformance Test Tools (self-test) against the
2FAK passkey firmware v5.0.0 (AAGUID c2aa81f8-352d-56e2-c689-2bfdb5a52ccc,
USB VID/PID 0x1209/0x2FA2).

Groups selected and passed: Transports, Generic, MakeCredential (Request and
Response), GetAssertion (Request and Response), Reset, Discoverable Credentials
(Resident Key), Extensions (HMAC-Secret, CredProtect, MinPinLength), ClientPIN
protocol 1, ClientPIN protocol 2, Credential Management API (including
updateUserInformation and deleteCredential), Metadata. Optional groups not applicable
to this device (Enterprise Attestation, Authenticator Config, Biometric Enrollment,
Large Blobs) were not selected.

> Self-test result using the public conformance tools. Formal FIDO Certification is a
> separate submission and is not claimed here.

---

# Passed Tests
Transports
Generic
MakeCredential Request
MakeCredential Response
GetAssertion Request
GetAssertion Response
Reset
Optional: Resident Key
Extensions
ClientPin protocol 1
ClientPin protocol 2
Credential Management API
Metadata tests

# Log
passes: 180failures: 0duration: 714.56s
HID-1 Test CTAP2 HID support
P-1 Open CTAPHID device, and listen for incoming data for at least three seconds. Authenticator MUST not send any data in idle mode. 3005ms‣
P-2 Send a random CONT frame to CTAPHID device, and listen for incoming data. Authenticator MUST NOT respond. 1431ms‣
P-3 Send valid CTAPHID_INIT, on channel CTAPHID_BROADCAST_CID(0xffffffff), wait for the response and check that: (a) response.CID MUST equal to request.CID (b) response.CMD MUST equal to request.CMD (e) response.CTAPHID MUST equal to 2 (f) response.DATA size MUST be 17 (g) parse response.DATA and: (1) check that DATA.NONCE(+0) equal to request.NONCE (2) check that DATA.NEWCID(+8) MUST not be 0x00 nor equal to CTAPHID_BROADCAST_CID(0xffffffff) (3) check that DATA.CTAPHIDPROTOCOL(+12) MUST be set to CTAPHID_IF_VERSION(2) (4) check that in DATA.CAPABILITIES(+16) flags, only CAPABILITY_WINK(0x01), CAPABILITY_WINK(0x02), CAPABILITY_CBOR(0x04) or CAPABILITY_NMSG(0x08) set (5) check that in DATA.CAPABILITIES(+16) flags that CAPABILITY_CBOR(0x04) is set 437ms‣
P-4 Send a three CTAPHID_INIT, on a CTAPHID_BROADCAST_CID(0xffffffff), wait for the responses and: (a) Recover CIDs from responses and check that they are unique. 1268ms‣
P-5 Send a single, valid CTAPHID_PING packet with a random payload, wait for the response, and check that response is equal to the request 871ms‣
P-6 Send a valid CTAPHID_PING, with a a large payload size(1024 bytes), and check that response is equal to the request 1456ms‣
P-7 Send a valid CTAPHID_PING, with a a large payload size(512 bytes) that has last continuation frame missing, and send instead CTAPHID_INIT command on the same CID, check that it's succeeds(i.e. aborts CTAPHID_PING) 906ms‣
P-8 Send a valid CTAPHID_PING, with data that contains a leding 0, and ensure that exact data is returned 1114ms‣
P-9 Send a valid MakeCredential request, and while its waiting for user action check that authenticator returns CTAPHID_KEEPALIVE(0x3B), that is 8 byte long, BCNT is 1 and DATA is either STATUS_PROCESSING(0x01) or STATUS_UPNEEDED(0x02) 4262ms‣
P-10 Send a valid MakeCredential request, and check that authenticator returns CTAPHID_KEEPALIVE(0x3B) while its waiting for user action. Send CTAPHID_CANCEL(0x11) and check that authenticator responds with CTAPHID_CBOR(0x90) with error code CTAP2_ERR_KEEPALIVE_CANCEL(0x2D). Send another CTAPHID_CANCEL(0x11) and check that authenticator does not respond with anything 3872ms‣
P-11 Send a valid GetAssertion request with invalid credId. Check that authenticator frist returns CTAPHID_KEEPALIVE(0x3B). Then waiting till authr finally returns CTAP2_ERR_NO_CREDENTIALS(0x2E) [SKIPPED FOR CTAP 2.1]
P-12 If CTAPHID_WINK(0x08) is implemented, send CTAPHID_WINK(0x08), wait for the response, and: (a) response.CMD is set to CTAPHID_WINK(0x08) (b) response.BCNT is set to 0 (c) response length is 7 bytes 870ms‣
P-13 If CTAPHID_LOCK(0x04) is implemented, send a valid CTAPHID_LOCK(0x04), with lock time set to 0, wait for the response, and check that: (a) response.CID MUST equal to request.CID (b) response.CMD MUST equal to request.CMD (c) response.BCNT MUST equal to 0 (d) response length must be 7 bytes
P-14 If CTAPHID_LOCK is implemented, send a valid CTAPHID_LOCK, with lock time set to 8 and: (b) while device is locked, send a valid CTAPHID_INIT, on the other CID, and wait for the response: (i) response.CMD MUST be set to CTAPHID_ERROR (ii) response.ERRORCODE(DATA[0]) MUST be set to CTAP1_ERR_CHANNEL_BUSY (c) send a valid CTAPHID_LOCK, with lock time set to 0 (b) end a valid CTAPHID_INIT, on the other CID, and wait for the response: (i) response.CID MUST equal to request.CID (ii) response.CMD MUST equal to request.CMD (iii) response.NONCE MUST equal to request.NONCE (iv) response.NEWCID MUST be presented (v) response.CTAPHID_VERSION MUST equal to CTAPHID_IF_VERSION(2) (vi) response.DATA size MUST be 17
P-15 Send a valid authenticatorSelection(0x0B) request, and check that authenticator returns CTAPHID_KEEPALIVE(0x3B) while its waiting for user action. Send CTAPHID_CANCEL(0x11) and check that authenticator responds with CTAPHID_CBOR(0x90) with error code CTAP2_ERR_KEEPALIVE_CANCEL(0x2D). Send another CTAPHID_CANCEL(0x11) and check that authenticator does not respond with anything 3163ms‣
F-1 Send an CTAPHID packet with an unknown command, and wait for the response: (a) response.CMD MUST be set to CTAPHID_ERROR (b) response.BCNT MUST equal to 1 (c) response.ERRORCODE(DATA[0]) MUST be set to CTAP1_ERR_INVALID_COMMAND 867ms‣
F-2 Send a valid CTAPHID_INIT packet, on a channel with id 0([0x00, 0x00, 0x00, 0x00]), and wait for the response: (a) response.CMD MUST be set to CTAPHID_ERROR (b) response.ERRORCODE(DATA[0]) MUST be set to CTAP1_ERR_INVALID_CHANNEL 421ms‣
F-3 Send a valid CTAPHID_PING packet, on a CID0 channel, and wait for the response: (a) response.CMD MUST be set to CTAPHID_ERROR (b) response.ERRORCODE(DATA[0]) MUST be set to CTAP1_ERR_INVALID_CHANNEL 438ms‣
F-4 Send a valid CTAPHID_PING, with a a large payload size(1024 bytes), that has a continuation frame with a SEQ that is out of order and: (a) response.CMD MUST be set to CTAPHID_ERROR (b) response.ERRORCODE(DATA[0]) MUST be set to ERR_INVALID_SEQ(0x04) 923ms‣
Authr-Generic-1 Generic FIDO protocol tests
P-1 Send a valid CTAP2 GetInfo request, wait for the response and check that: (a) CTAP2 responseCode is CTAP1_ERR_SUCCESS(0x00) (b) Check that 'version' (0x01) field is presented and is set to "FIDO_2_3" (c) If GetInfo contains 'extensions' (0x02) field, check that its of type SEQUENCE, and only contains STRINGS (d) Check that 'aaguid' (0x03) field is presented and is 16 bytes long & Check that aaguid(0x03) field value matches Metadata.aaguid (e) If GetInfo contains 'options' (0x04) field, check that its of type MAP (f) If GetInfo contains 'maxMsgSize' (0x05) field, check that its of type NUMBER (g) If GetInfo contains 'pinUvAuthProtocols' (0x06) field, check that its of type SEQUENCE, and only contains NUMBERS (h) If GetInfo contains 'maxCredentialCountInList' (0x07) check that its of type NUMBER, greater than 0 (i) If GetInfo contains 'maxCredentialIdLength' (0x08) check that its of type NUMBER, greater than 0 (j) If GetInfo contains 'transports' (0x09) check that its of type SEQUENCE, and only contains STRINGS (k) If GetInfo contains 'algorithms'(0x0A) check that its of type SEQUENCE, and not empty, and doesn't contain duplicates (l) If GetInfo contains 'maxSerializedLargeBlobArray'(0x0B) (m) If GetInfo contains 'forcePINChange'(0x0C) (n) If GetInfo contains 'minPINLength'(0x0D) (o) If GetInfo contains 'firmwareVersion'(0x0E) (p) If GetInfo contains 'maxCredBlobLength'(0x0F) (q) If GetInfo contains 'maxRPIDsForSetMinPINLength'(0x10) (r) If GetInfo contains 'preferredPlatformUvAttempts'(0x11) (s) If GetInfo contains 'uvModality'(0x12) (t) If GetInfo contains 'certifications'(0x13) (u) If GetInfo contains 'remainingDiscoverableCredentials'(0x14) (v) If GetInfo contains 'vendorPrototypeConfigCommands'(0x15) (w) If GetInfo contains 'attestationFormats'(0x16) (x) If GetInfo contains 'uvCountSinceLastPinEntry'(0x17) (y) If GetInfo contains 'longTouchForReset'(0x18) (z) If GetInfo contains 'encIdentifier'(0x19) (a) If GetInfo contains 'transportsForReset'(0x1A) (b) If GetInfo contains 'pinComplexityPolicy'(0x1B) (c) If GetInfo contains 'pinComplexityPolicyURL'(0x1C) (d) If GetInfo contains 'maxPINLength'(0x1D) (---) Check that GetInfo response equals to the metadata.authenticatorGetInfo => Included within relevant block of (*) lettered test‣
P-2 If GetInfo contains Options field: Check that every option in options is of type Boolean. Additionally: (a) If "up" is set to true, check that metadata.userVerificationDetails contains VerificationMethodDescriptor that has "userVerification" set to "presence_internal" (b) If "uv" is set to true, check that metadata.userVerificationDetails contains VerificationMethodDescriptor that has "userVerification" set to either of "fingerprint_internal", "voiceprint_internal", "faceprint_internal", "eyeprint_internal", "handprint_internal", "pattern_internal", "pattern_external", "passcode_internal", "passcode_external" (c) If "uv" and "up" are false, check that metadata.userVerificationDetails contains VerificationMethodDescriptor that has "userVerification" set to "none"‣
P-3 If GetInfo contains pinUvAuthProtocols, and it is not empty, check that Metadata.userVerificationDetails contains VerificationMethodDescriptor set to "passcode_external"‣
P-4 If GetInfo contains encIdentifier, and it is not empty, Send getInfo request and check that it regenerates new encryption iv for the device identifier, Validate that decrypted encIdentifier stays same between getInfo calls, Send Reset and check that it generates new random value for the device identifier (ct)
P-5 If GetInfo contains encCredStoreState, and it is not empty, Send getInfo request and check that it regenerates new encryption iv for the credential store state Validate that decrypted encCredStoreState stays same between getInfo calls, Send Reset and check that it generates new random value for the credential store state (ct)
Authr-MakeCred-Req-1 Test authenticatorMakeCredential(0x01) request MAP
P-1 Send a valid CTAP2 authenticatorMakeCredential(0x01) message, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. 2075ms‣
F-1 Send CTAP2 authenticatorMakeCredential(0x01) message, with "clientDataHash" is missing, wait for the response, and check that Authenticator returns an error. 873ms‣
F-2 Send CTAP2 authenticatorMakeCredential(0x01) message, with "clientDataHash" is NOT of type BYTE ARRAY, wait for the response, and check that Authenticator returns an error. 891ms‣
F-3 Send CTAP2 authenticatorMakeCredential(0x01) message, with "rp" is missing, wait for the response, and check that Authenticator returns an error. 877ms‣
F-4 Send CTAP2 authenticatorMakeCredential(0x01) message, with "rp" is NOT of type MAP, wait for the response, and check that Authenticator returns an error. 884ms‣
F-5 Send CTAP2 authenticatorMakeCredential(0x01) message, with "user" is missing, wait for the response, and check that Authenticator returns an error. 874ms‣
F-6 Send CTAP2 authenticatorMakeCredential(0x01) message, with "user" is NOT of type MAP, wait for the response, and check that Authenticator returns an error. 912ms‣
F-7 Send CTAP2 authenticatorMakeCredential(0x01) message, with "pubKeyCredParams" is missing, wait for the response, and check that Authenticator returns an error. 897ms‣
F-8 Send CTAP2 authenticatorMakeCredential(0x01) message, with "pubKeyCredParams" is NOT of type ARRAY, wait for the response, and check that Authenticator returns an error. 891ms‣
F-9 Send CTAP2 authenticatorMakeCredential(0x01) message, with "excludeList" that is NOT of type SEQUENCE, wait for the response, and check that Authenticator returns an error. 879ms‣
F-10 Send CTAP2 authenticatorMakeCredential(0x01) message, with "extensions" that is NOT of type MAP, wait for the response, and check that Authenticator returns an error. 863ms‣
F-11 Send CTAP2 authenticatorMakeCredential(0x01) message, with "options" that is NOT of type MAP, wait for the response, and check that Authenticator returns an error. 876ms‣
Authr-MakeCred-Req-2 Test PublicKeyCredentialRpEntity in MakeCredential request
F-1 Send CTAP2 authenticatorMakeCredential(0x01) message, with "rp.id" is NOT of type TEXT, wait for the response, and check that Authenticator returns an error. 888ms‣
F-2 Send CTAP2 authenticatorMakeCredential(0x01) message, with "rp.name" is NOT of type TEXT, wait for the response, and check that Authenticator returns an error. 861ms‣
Authr-MakeCred-Req-3 Test PublicKeyCredentialUserEntity in MakeCredential request
F-1 Send CTAP2 authenticatorMakeCredential(0x01) message, with "user.id" is NOT of type BYTE ARRAY, wait for the response, and check that Authenticator returns an error. 859ms‣
F-2 Send CTAP2 authenticatorMakeCredential(0x01) message, with "user.name" is NOT of type TEXT, wait for the response, and check that Authenticator returns an error. 856ms‣
F-3 Send CTAP2 authenticatorMakeCredential(0x01) message, with "user.displayName" is NOT of type TEXT, wait for the response, and check that Authenticator returns an error. 875ms‣
Authr-MakeCred-Req-4 Test PubKeyCredParams Sequence in MakeCredential request
P-1 Send CTAP2 authenticatorMakeCredential(0x01) message, with "pubKeyCredParams" with unsupported algorithm listed before supported algorithms, wait for the response, check that Authenticator selects the first supported algorithm and returns CTAP1_ERR_SUCCESS(0x00) error code. 2080ms‣
F-1 Send CTAP2 authenticatorMakeCredential(0x01) message, with "pubKeyCredParams" contains an item of type NOT a MAP, wait for the response, and check that Authenticator returns an error. 873ms‣
F-2 Send CTAP2 authenticatorMakeCredential(0x01) message, with "pubKeyCredParams" contains a "PublicKeyCredentialParameters" with "type" field that is missing, wait for the response, and check that Authenticator returns an error. 863ms‣
F-3 Send CTAP2 authenticatorMakeCredential(0x01) message, with "pubKeyCredParams" contains a "PublicKeyCredentialParameters" with "type" field that is NOT of type TEXT, wait for the response, and check that Authenticator returns an error. 858ms‣
F-4 Send CTAP2 authenticatorMakeCredential(0x01) message, with "pubKeyCredParams" contains a "PublicKeyCredentialParameters" with "alg" field is missing, wait for the response, and check that Authenticator returns an error. 863ms‣
F-5 Send CTAP2 authenticatorMakeCredential(0x01) message, with "pubKeyCredParams" contains a "PublicKeyCredentialParameters" with "alg" is NOT of type INTEGER, wait for the response, and check that Authenticator returns an error. 857ms‣
F-6 Send CTAP2 authenticatorMakeCredential(0x01) message, with "pubKeyCredParams", only containing a "PublicKeyCredentialParameters" with "alg" set to unsupported by the authenticator algorithm, wait for the response, and check that Authenticator returns error CTAP2_ERR_UNSUPPORTED_ALGORITHM(0x26). 871ms‣
F-7 Send CTAP2 authenticatorMakeCredential(0x01) message, with "pubKeyCredParams" contains a "PublicKeyCredentialParameters" with "type" is NOT set to "public-key", wait for the response, and check that Authenticator returns error CTAP2_ERR_UNSUPPORTED_ALGORITHM(0x26). 858ms‣
Authr-MakeCred-Req-5 Test ExcludeList in MakeCredential request
P-1 Send a valid CTAP2 authenticatorMakeCredential(0x01) message, with "excludeList" that contains "PublicKeyCredentialDescriptor" with "type" field is NOT set to "public-key", wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. 2069ms‣
P-2 Send a valid CTAP2 authenticatorMakeCredential(0x01) message, with "attestationFormatsPreference" that contains Array of String set to attestation statement format identifiers, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. 2077ms‣
P-3 Send a valid CTAP2 authenticatorMakeCredential(0x01) message, with "attestationFormatsPreference" that contains Array of String set to "none", wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. 2054ms‣
P-4 Send CTAP2 authenticatorMakeCredential(0x01) message, with "attestationFormatsPreference" that contains an element that is NOT of type Array, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. 2061ms‣
F-1 Send CTAP2 authenticatorMakeCredential(0x01) message, with "excludeList" that contains an element that is NOT of type MAP, wait for the response, and check that Authenticator returns an error. 889ms‣
F-2 Send CTAP2 authenticatorMakeCredential(0x01) message, with "excludeList" that contains "PublicKeyCredentialDescriptor" with "type" field is missing, wait for the response, and check that Authenticator returns an error. 878ms‣
F-3 Send CTAP2 authenticatorMakeCredential(0x01) message, with "excludeList" that contains "PublicKeyCredentialDescriptor" with "type" field is NOT of type TEXT, wait for the response, and check that Authenticator returns an error. 865ms‣
F-5 Send CTAP2 authenticatorMakeCredential(0x01) message, with "excludeList" that contains "PublicKeyCredentialDescriptor" with "id" field is missing, wait for the response, and check that Authenticator returns an error. 874ms‣
F-6 Send CTAP2 authenticatorMakeCredential(0x01) message, with "excludeList" that contains "PublicKeyCredentialDescriptor" with "id" field is NOT of type ARRAY BUFFER, wait for the response, and check that Authenticator returns an error. 873ms‣
F-7 Send CTAP2 authenticatorMakeCredential(0x01) message, with "excludeList" that contains "PublicKeyCredentialDescriptor" with "id" of the previously registered credential, wait for the response, and check that Authenticator returns an error CTAP2_ERR_CREDENTIAL_EXCLUDED(0x19). 5363ms‣
Authr-MakeCred-Req-6 Test Options in MakeCredential request
P-1 Send a valid CTAP2 authenticatorMakeCredential(0x01) message, "options" containing an unknown option, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. 2057ms‣
P-2 If authenticator supports "uv" option, send a valid CTAP2 authenticatorMakeCredential(0x01) message, options.uv set to true, wait for the response, check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code, and check that authenticatorData.flags have UV flag set
P-3 If authenticator supports "up" option, send a valid CTAP2 authenticatorMakeCredential(0x01) message, options.up set to true, wait for the response, check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code, and check that authenticatorData.flags have UP flag set 2060ms‣
F-1 Send a valid CTAP2 authenticatorMakeCredential(0x01) message, options.up set to false, wait for the response, check that Authenticator returns an error CTAP2_ERR_INVALID_OPTION(0x2C) 875ms‣
Authr-MakeCred-Resp-1 Test registration response, and "packed" attestation
P-01 Send a valid CTAP2 authenticatorMakeCredential(0x01) message, wait for the response, and check that: (a) Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code (b) Response structure can be successfully parsed (c) Response.fmt is of type String (d) Response.fmt is set to "packed"‣
P-02 Using previously received response, check that: (a) response.authData is of type BYTE ARRAY (b) check that authData is at least (32 + 1 + 4 + 16 + 2 + 16 + 42) bytes long. (c) parse response.authData (d) check that AAGUID matching the one in metadata statement (e) check that authData.rpIdHash matches the sent rpIdHash (f) check that UP(bit 0) flag in flags is set (g) check that AT(bit 6) flag in flags is set, and attestation credential data is presented (h) check that ED(bit 7) flag in flags is not set, and check that there is not Extension Data present (i) check that authData.pubKey is correctly encoded: (j) if public key is an RSA(kty(1) is set to 3) public key, check that: (1) alg(2) is set to algorithm that matches corresponding one in metadata statement (2) contains n(-1) that is of type BYTE STRING (3) contains e(-2) that is of type BYTE STRING (4) does NOT contains ANY other coefficients (k) if public key is an EC2(kty(1) is set to 2) public key, check that: (1) alg(2) is set to algorithm that matches corresponding one in metadata statement (2) crv(-1) field that is set to EC identifier from "COSE Elliptic Curves" registry (3) contains x(-2) is of type BYTE STRING, and is 32bytes long (4) contains y(-3) is of type BYTE STRING, and is 32bytes long (5) does NOT contains ANY other coefficients (k) if public key is an OKP(kty(1) is set to 1) public key, check that: (1) alg(2) is set to algorithm that matches corresponding one in metadata statement (2) crv(-1) field that is set to EdDSA identifier from "COSE Elliptic Curves" registry (3) contains x(-2) is of type BYTE STRING, and is 32bytes long (5) does NOT contains ANY other coefficients‣
P-03 If "attStmt" field presented: Decode "attStmt" CBOR MAP and: (a) check that "alg" field is presented it matches "alg" in PK (b) check that "sig" field is presented and it is of type BYTE STRING‣
P-04 If "x5c" presented: (b) Check that "x5c" is of type SEQUENCE (c) Check that metadata statement contains "attestationRootCertificates" field, and it’s not empty. (d) Check that metadata statement "attestationTypes" SEQUENCE contains "basic_full" (e) Decode certificate chain (f) If "x5c" contains exactly one certificate, check if that certificate is already a member of the metadata.attestationRootCertificates. If it is, skip chain verification (g) Verify certificate chain: (1) For each certificate in the metadata.attestationRootCertificates, attach attestation root to the x5c, and try verifying it. If none produce a valid chain, fail (h) Pick a leaf certificate of the chain and check that: (1) Version is of type INTEGER and is set to 3 (2) Subject-C - is of type UTF8String, and is set to ISO 3166 code specifying the country where the Authenticator vendor is incorporated (UTF8String) (3) Subject-O - is of type UTF8String, and is set to the legal name of the Authenticator vendor (4) Subject-OU - is of type UTF8String, and is set to literal string “Authenticator Attestation” (5) Subject-CN - is of type UTF8String, and is not empty (6) Basic Constraints extension MUST have the CA component set to false. (7) [TBD] If the related attestation root certificate is used for multiple authenticator models, the Extension OID 1.3.6.1.4.1.45724.1.1.4 (id-fido-gen-ce-aaguid) MUST be present, containing the AAGUID as a 16-byte OCTET STRING. The extension MUST NOT be marked as critical. (8) Check that certificate is not expired, is current(notBefore is set to the past date), and is valid for at least 5 years [TBD] (i) Concatenate authenticatorData and signData to clientDataHash. Using key extracted from leaf certificate, signData verify signature in "sig" field.‣
P-06 If "x5c" is not present, then self attestation is presented, then: (a) Check that metadata statement contains "attestationRootCertificates" field, and it’s an empty ARRAY. (b) Check that metadata statement attestationFormats contains "basic_surrogate" (c) Concatenate authenticatorData and clientDataHash to signData. Using public key extracted from authenticatorData, signData verify signature in "sig" field.
F-01 Using previously received response, check that: (a) unsignedExtensionOutputs field is undefined‣
Authr-GetAssertion-Req-1 Test getAssertion request
P-1 Send a valid CTAP2 authenticatorGetAssertion(0x02) message, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. 1563ms‣
F-1 Send CTAP2 authenticatorGetAssertion(0x02) message, with "rpId" is missing, wait for the response, and check that Authenticator returns an error. 894ms‣
F-2 Send CTAP2 authenticatorGetAssertion(0x02) message, with "rpId" is NOT of type STRING, wait for the response, and check that Authenticator returns an error. 861ms‣
F-3 Send CTAP2 authenticatorGetAssertion(0x02) message, with "clientDataHash" is missing, wait for the response, and check that Authenticator returns an error. 856ms‣
F-4 Send CTAP2 authenticatorGetAssertion(0x02) message, with "clientDataHash" is NOT of type BYTE ARRAY, wait for the response, and check that Authenticator returns an error. 872ms‣
F-5 Send CTAP2 authenticatorGetAssertion(0x02) message, with "allowList" is NOT of type ARRAY, wait for the response, and check that Authenticator returns an error. 860ms‣
F-6 Send CTAP2 authenticatorGetAssertion(0x02) message, with "allowList" contains a credential that is NOT of type MAP, wait for the response, and check that Authenticator returns an error. 853ms‣
Authr-GetAssertion-Req-2 Test "options" in GetAssertion request
P-1 Send a valid CTAP2 authenticatorGetAssertion(0x02) message, "options" contain an unknown option, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. 1617ms‣
P-2 If authenticator supports "up" option, send a valid CTAP2 authenticatorGetAssertion(0x02) message, options.up set to true, wait for the response, check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code, and check that authenticatorData.flags have UP flag set 1567ms‣
P-3 If authenticator supports "uv" option, send a valid CTAP2 authenticatorGetAssertion(0x02) message, options.uv set to true, wait for the response, check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code, and check that authenticatorData.flags have UV flag set
Authr-GetAssertion-Req-3 Test "allowList" field in MakeCredential request
P-1 Send a valid CTAP2 authenticatorGetAssertion(0x02) message, with "allowList" that contains "PublicKeyCredentialDescriptor" with "type" field is NOT set to "public-key", wait for the response, and check that authenticator returns CTAP1_ERR_SUCCESS(0x00) error code 1590ms‣
F-1 Send CTAP2 authenticatorGetAssertion(0x02) message, with "allowList" that contains an element that is NOT of type MAP, wait for the response, and check that Authenticator returns an error. 868ms‣
F-2 Send CTAP2 authenticatorGetAssertion(0x02) message, with "allowList" that contains "PublicKeyCredentialDescriptor" with "type" field is missing, wait for the response, and check that Authenticator returns an error. 866ms‣
F-3 Send CTAP2 authenticatorGetAssertion(0x02) message, with "allowList" that contains "PublicKeyCredentialDescriptor" with "type" field is NOT of type TEXT, wait for the response, and check that Authenticator returns an error. 862ms‣
F-4 Send CTAP2 authenticatorGetAssertion(0x02) message, with "allowList" that contains "PublicKeyCredentialDescriptor" with "id" field is missing, wait for the response, and check that Authenticator returns an error. 848ms‣
F-5 Send CTAP2 authenticatorGetAssertion(0x02) message, with "allowList" that contains "PublicKeyCredentialDescriptor" with "id" field is NOT of type ARRAY BUFFER, wait for the response, and check that Authenticator returns an error. 859ms‣
F-6 If authenticator is Second-Factor only: Send CTAP2 authenticatorGetAssertion(0x02) message, with missing "allowList", and check that authenticator returns CTAP2_ERR_NO_CREDENTIALS(0x2E) error code.
Authr-GetAssertion-Resp-1.js Test GetAssertion response
P-1 Parse GetAssertion response, and check that: (a) response includes "credential" field, and it contains fields "type" and "id" (b) response includes "authData" field, and it's of type BYTE STRING (c) response includes "signature" field, and it's of type BYTE STRING‣
P-2 Parse GetAssertion_Response.authData and: (a) Check that it's exactly 37 bytes long (b) Check that authData.rpIdHash matches the hash of the GetAssertion_Request.rpId (c) Check that AT flag in authData.flags bitmap is not set‣
P-3 Send three valid CTAP2 authenticatorGetAssertion(0x02) request, wait for the responses, and check that response2.counter is bigger than response1.counter, and response3.counter is bigger than response2.counter. 10683ms‣
P-4 Merge authData and clientDataHash, and using previously acquired publicKey verify signature from GetAssertion_Response 58ms‣
F-1 Parse GetAssertion_Response.unsignedExtensionOutputs and: (a) Check that unsignedExtensionOutputs field is undefined‣
Authr-Reset-1 Test authenticator support of Reset command
P-1 Successfully execute makeCredential, and test it by sending consequent getAssertion and check that both are succeeding. Send authenticatorReset(0x07) immediately after, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. Send a valid CTAP2 authenticatorGetAssertion(0x02) message, with credId from the previously registered makeCredential, wait for the response, and check that Authenticator returns CTAP2_ERR_NO_CREDENTIALS(0x2E) error code. 16251ms‣
Discoverable Credentials(Resident-Key) Test Discoverable Credentials(Resident-Key) support
P-1 Send a valid CTAP2 authenticatorMakeCredential(0x01) message, "options" containing an "rk" option set to true, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. 2143ms‣
P-2 FOR AUTHENTICATORS WITHOUT A DISPLAY AND PERFORM NO VERIFICATION Send three valid CTAP2 authenticatorMakeCredential(0x01) message, "options" containing an "rk" option set to true, and if authenticator supports UV option set "uv" to false, wait for the responses, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. Send a valid CTAP2 authenticatorGetAssertion(0x02) message, with no allowList presented, wait for the response and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. Check that response contains "numberOfCredentials" field that is of type Number and is set to 3. Send authenticatorGetNextAssertion(0x08), until numberOfCredentials is 1, retrieve responses and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code for each of the requests. Check that response.user ONLY contains id field and nothing else! 9569ms‣
P-3 FOR AUTHENTICATORS WITHOUT A DISPLAY AND USE EITHER UV OR CLIENTPIN If UV option is supported, configure UV, else set new pin, and run registrations with pin. Send two valid CTAP2 authenticatorMakeCredential(0x01) message, "options" containing an "rk" option set to true, and if authenticator supports UV option set "uv" to true, wait for the responses, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. Send a valid CTAP2 authenticatorGetAssertion(0x02) message, with no allowList presented, wait for the response and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. Check that response contains "numberOfCredentials" field that is of type Number and is set to 2. Send authenticatorGetNextAssertion(0x08), until numberOfCredentials is 1, retrieve responses and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code for each of the requests. Check that response.user contains all of the registered userInfo 15410ms‣
P-4 FOR AUTHENTICATORS WITH DISPLAY Send three valid CTAP2 authenticatorMakeCredential(0x01) message, "options" containing an "rk" option set to true, wait for the responses, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. Send three CTAP2 authenticatorGetAssertion(0x02) messages, with no allowList presented, asking using in a random order to select credentials, wait for the response and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. Check that response contains "numberOfCredentials" field that is of type Number and is set to 1.
P-5 If GetInfo contains encCredStoreState, and it is not empty, Send a valid CTAP2 authenticatorMakeCredential(0x01) message, "options" containing an "rk" option set to true, Сheck that it generates new random value for the credential store state (ct) Send getInfo request and check that it regenerates new encryption iv for the credential store state
P-6 Create a valid CTAP2 credential, with an "rk" option set to true. For the same Relying Party Identifier, overwrite previous credential with new valid rk credential, and check that both values of credId and COSEPublicKey, was changed. 8204ms‣
HMAC-Secret Test HMAC-Secret extension support
Tests for discoverable credentials.
P-1 Send a valid CTAP2 authenticatorMakeCredential(0x01) message, "extensions" containing a valid "hmac-secret" set to true, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code, with extensions payload containing 'hmac-secret' field set to true 4020ms‣
P-2 Send a valid CTAP2 getAssertion(0x02) message, "extensions" containing a valid "hmac-secret" extension request, with one salt, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code, and: (a) Check that response contains extensions encrypted "hmac-secret" extension response. Decrypt it and save it as salt1 (b) Send another GetAssertion with salt1 and salt2, and check that response still equal to result, and nonUvSalt2Hmac does not equal nonUvSalt1Hmac 8259ms‣
P-3 Send a valid CTAP2 GetAssertion(0x02) message, "extensions" containing a valid "hmac-secret" extension request, with salt1 and salt2, wait for the response, and: (a) Check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code (b) Check that response extensions contain "hmac-secret" extension. Decrypt extensions (c) Check that decrypted hmacs contain uvSalt1Hmac, and uvSalt2Hmac (d) Check that uvSalt1Hmac does not equal to nonUvSalt1Hmac, an uvSalt2Hmac does not equal to nonUvSalt2Hmac 5691ms‣
F-1 Send a valid CTAP2 authenticatorMakeCredential(0x01) message, "extensions" containing "hmac-secret" set to a random type, wait for the response, and check that Authenticator returns an error 3778ms‣
F-2 Send a CTAP2 getAssertion(0x02) message, with "extensions" containing "hmac-secret" extension request with one salt that is shorter than 32 bytes, wait for the response, and check that authenticator returns an error 3757ms‣
F-3 Send a CTAP2 getAssertion(0x02) message, with "extensions" containing a "hmac-secret" extension request with two salts, with second salt that is shorter than 32 bytes, wait for the response, and check that authenticator returns an error 4655ms‣
Tests for non-discoverable credentials.
P-1 Send a valid CTAP2 authenticatorMakeCredential(0x01) message, "extensions" containing a valid "hmac-secret" set to true, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code, with extensions payload containing 'hmac-secret' field set to true 4101ms‣
P-2 Send a valid CTAP2 getAssertion(0x02) message, "extensions" containing a valid "hmac-secret" extension request, with one salt, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code, and: (a) Check that response contains extensions encrypted "hmac-secret" extension response. Decrypt it and save it as salt1 (b) Send another GetAssertion with salt1 and salt2, and check that response still equal to result, and nonUvSalt2Hmac does not equal nonUvSalt1Hmac 8233ms‣
P-3 Send a valid CTAP2 GetAssertion(0x02) message, "extensions" containing a valid "hmac-secret" extension request, with salt1 and salt2, wait for the response, and: (a) Check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code (b) Check that response extensions contain "hmac-secret" extension. Decrypt extensions (c) Check that decrypted hmacs contain uvSalt1Hmac, and uvSalt2Hmac (d) Check that uvSalt1Hmac does not equal to nonUvSalt1Hmac, an uvSalt2Hmac does not equal to nonUvSalt2Hmac 5665ms‣
F-1 Send a valid CTAP2 authenticatorMakeCredential(0x01) message, "extensions" containing "hmac-secret" set to a random type, wait for the response, and check that Authenticator returns an error 3812ms‣
F-2 Send a CTAP2 getAssertion(0x02) message, with "extensions" containing "hmac-secret" extension request with one salt that is shorter than 32 bytes, wait for the response, and check that authenticator returns an error 3769ms‣
F-3 Send a CTAP2 getAssertion(0x02) message, with "extensions" containing a "hmac-secret" extension request with two salts, with second salt that is shorter than 32 bytes, wait for the response, and check that authenticator returns an error 4393ms‣
HMAC-Secret: Strict PUAT2 Test authenticators for support of HMAC-Secret extension in strictly PinUvAuth Protocol 2 setting.
Tests for discoverable credentials.
P-1 Send a valid CTAP2 authenticatorMakeCredential(0x01) message, "extensions" containing a valid "hmac-secret" set to true, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code, with extensions payload containing 'hmac-secret' field set to true 4024ms‣
P-2 Send a valid CTAP2 getAssertion(0x02) message, "extensions" containing a valid "hmac-secret" extension request, with one salt, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code, and: (a) Check that response contains extensions encrypted "hmac-secret" extension response. Decrypt it and save it as salt1 (b) Send another GetAssertion with salt1 and salt2, and check that response still equal to result, and nonUvSalt2Hmac does not equal nonUvSalt1Hmac 8216ms‣
P-3 Send a valid CTAP2 GetAssertion(0x02) message, "extensions" containing a valid "hmac-secret" extension request, with salt1 and salt2, wait for the response, and: (a) Check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code (b) Check that response extensions contain "hmac-secret" extension. Decrypt extensions (c) Check that decrypted hmacs contain uvSalt1Hmac, and uvSalt2Hmac (d) Check that uvSalt1Hmac does not equal to nonUvSalt1Hmac, an uvSalt2Hmac does not equal to nonUvSalt2Hmac 5662ms‣
F-1 Send a valid CTAP2 authenticatorMakeCredential(0x01) message, "extensions" containing "hmac-secret" set to a random type, wait for the response, and check that Authenticator returns an error 3797ms‣
F-2 Send a CTAP2 getAssertion(0x02) message, with "extensions" containing "hmac-secret" extension request with one salt that is shorter than 32 bytes, wait for the response, and check that authenticator returns an error 4428ms‣
F-3 Send a CTAP2 getAssertion(0x02) message, with "extensions" containing a "hmac-secret" extension request with two salts, with second salt that is shorter than 32 bytes, wait for the response, and check that authenticator returns an error 4423ms‣
Tests for non-discoverable credentials.
P-1 Send a valid CTAP2 authenticatorMakeCredential(0x01) message, "extensions" containing a valid "hmac-secret" set to true, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code, with extensions payload containing 'hmac-secret' field set to true 4061ms‣
P-2 Send a valid CTAP2 getAssertion(0x02) message, "extensions" containing a valid "hmac-secret" extension request, with one salt, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code, and: (a) Check that response contains extensions encrypted "hmac-secret" extension response. Decrypt it and save it as salt1 (b) Send another GetAssertion with salt1 and salt2, and check that response still equal to result, and nonUvSalt2Hmac does not equal nonUvSalt1Hmac 8238ms‣
P-3 Send a valid CTAP2 GetAssertion(0x02) message, "extensions" containing a valid "hmac-secret" extension request, with salt1 and salt2, wait for the response, and: (a) Check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code (b) Check that response extensions contain "hmac-secret" extension. Decrypt extensions (c) Check that decrypted hmacs contain uvSalt1Hmac, and uvSalt2Hmac (d) Check that uvSalt1Hmac does not equal to nonUvSalt1Hmac, an uvSalt2Hmac does not equal to nonUvSalt2Hmac 5731ms‣
F-1 Send a valid CTAP2 authenticatorMakeCredential(0x01) message, "extensions" containing "hmac-secret" set to a random type, wait for the response, and check that Authenticator returns an error 3799ms‣
F-2 Send a CTAP2 getAssertion(0x02) message, with "extensions" containing "hmac-secret" extension request with one salt that is shorter than 32 bytes, wait for the response, and check that authenticator returns an error 4416ms‣
F-3 Send a CTAP2 getAssertion(0x02) message, with "extensions" containing a "hmac-secret" extension request with two salts, with second salt that is shorter than 32 bytes, wait for the response, and check that authenticator returns an error 4704ms‣
HMAC-Secret Test HMAC Secret MakeCredential Extension support
Tests for pinUvAuthProtocol: 1
Tests for discoverable/non-discoverable. Curent rk: false
P-1 For authenticator with makeCredUvNotRqd:true, Send a valid CTAP2 authenticatorMakeCredential(0x01) message without UV, with salt1 and salt2 in "hmac-secret-mc". Send a valid CTAP2 GetAssertion(0x02) message with UV, with salt1 and salt2 in "hmac-secret". Check that hmac secrets from non-UV request are different from UV request.
P-2 Send a valid CTAP2 authenticatorMakeCredential(0x01) message with "hmac-secret-mc" with salt1. Send a valid CTAP2 GetAssertion(0x02) message, with extensions that contain a valid "hmac-secret" extension request with salt1. Check that returned hmac secret equals to the hamc secret from MakeCredential.
P-3 Send a valid CTAP2 authenticatorMakeCredential(0x01) message with "hmac-secret-mc" with salt1 and salt2. Send a valid CTAP2 GetAssertion(0x02) message, with extensions that contain a valid "hmac-secret" extension request with salt1 and salt2. Check that returned hmac secrets are equal to the hamc secrets from MakeCredential.
F-1 Send a CTAP2 authenticatorMakeCredential(0x01) message, without necessary "hmac-secret" in extensions Check that Authenticator returns CTAP2_ERR_MISSING_PARAMETER(0x14) error code
F-2 Send a valid CTAP2 authenticatorMakeCredential(0x01) message, "extensions" containing "hmac-secret-mc" set to a random types, wait for the response, and check that Authenticator returns an error
F-3 Send a CTAP2 getAssertion(0x02) message, with "extensions" containing "hmac-secret-mc" extension request with one salt that is shorter than 32 bytes, wait for the response, and check that authenticator returns an error
F-4 Send a CTAP2 getAssertion(0x02) message, with "extensions" containing a "hmac-secret-mc" extension request with two salts, with second salt that is shorter than 32 bytes, wait for the response, and check that authenticator returns an error
Tests for discoverable/non-discoverable. Curent rk: true
P-1 For authenticator with makeCredUvNotRqd:true, Send a valid CTAP2 authenticatorMakeCredential(0x01) message without UV, with salt1 and salt2 in "hmac-secret-mc". Send a valid CTAP2 GetAssertion(0x02) message with UV, with salt1 and salt2 in "hmac-secret". Check that hmac secrets from non-UV request are different from UV request.
P-2 Send a valid CTAP2 authenticatorMakeCredential(0x01) message with "hmac-secret-mc" with salt1. Send a valid CTAP2 GetAssertion(0x02) message, with extensions that contain a valid "hmac-secret" extension request with salt1. Check that returned hmac secret equals to the hamc secret from MakeCredential.
P-3 Send a valid CTAP2 authenticatorMakeCredential(0x01) message with "hmac-secret-mc" with salt1 and salt2. Send a valid CTAP2 GetAssertion(0x02) message, with extensions that contain a valid "hmac-secret" extension request with salt1 and salt2. Check that returned hmac secrets are equal to the hamc secrets from MakeCredential.
F-1 Send a CTAP2 authenticatorMakeCredential(0x01) message, without necessary "hmac-secret" in extensions Check that Authenticator returns CTAP2_ERR_MISSING_PARAMETER(0x14) error code
F-2 Send a valid CTAP2 authenticatorMakeCredential(0x01) message, "extensions" containing "hmac-secret-mc" set to a random types, wait for the response, and check that Authenticator returns an error
F-3 Send a CTAP2 getAssertion(0x02) message, with "extensions" containing "hmac-secret-mc" extension request with one salt that is shorter than 32 bytes, wait for the response, and check that authenticator returns an error
F-4 Send a CTAP2 getAssertion(0x02) message, with "extensions" containing a "hmac-secret-mc" extension request with two salts, with second salt that is shorter than 32 bytes, wait for the response, and check that authenticator returns an error
Tests for pinUvAuthProtocol: 2
Tests for discoverable/non-discoverable. Curent rk: false
P-1 For authenticator with makeCredUvNotRqd:true, Send a valid CTAP2 authenticatorMakeCredential(0x01) message without UV, with salt1 and salt2 in "hmac-secret-mc". Send a valid CTAP2 GetAssertion(0x02) message with UV, with salt1 and salt2 in "hmac-secret". Check that hmac secrets from non-UV request are different from UV request.
P-2 Send a valid CTAP2 authenticatorMakeCredential(0x01) message with "hmac-secret-mc" with salt1. Send a valid CTAP2 GetAssertion(0x02) message, with extensions that contain a valid "hmac-secret" extension request with salt1. Check that returned hmac secret equals to the hamc secret from MakeCredential.
P-3 Send a valid CTAP2 authenticatorMakeCredential(0x01) message with "hmac-secret-mc" with salt1 and salt2. Send a valid CTAP2 GetAssertion(0x02) message, with extensions that contain a valid "hmac-secret" extension request with salt1 and salt2. Check that returned hmac secrets are equal to the hamc secrets from MakeCredential.
F-1 Send a CTAP2 authenticatorMakeCredential(0x01) message, without necessary "hmac-secret" in extensions Check that Authenticator returns CTAP2_ERR_MISSING_PARAMETER(0x14) error code
F-2 Send a valid CTAP2 authenticatorMakeCredential(0x01) message, "extensions" containing "hmac-secret-mc" set to a random types, wait for the response, and check that Authenticator returns an error
F-3 Send a CTAP2 getAssertion(0x02) message, with "extensions" containing "hmac-secret-mc" extension request with one salt that is shorter than 32 bytes, wait for the response, and check that authenticator returns an error
F-4 Send a CTAP2 getAssertion(0x02) message, with "extensions" containing a "hmac-secret-mc" extension request with two salts, with second salt that is shorter than 32 bytes, wait for the response, and check that authenticator returns an error
Tests for discoverable/non-discoverable. Curent rk: true
P-1 For authenticator with makeCredUvNotRqd:true, Send a valid CTAP2 authenticatorMakeCredential(0x01) message without UV, with salt1 and salt2 in "hmac-secret-mc". Send a valid CTAP2 GetAssertion(0x02) message with UV, with salt1 and salt2 in "hmac-secret". Check that hmac secrets from non-UV request are different from UV request.
P-2 Send a valid CTAP2 authenticatorMakeCredential(0x01) message with "hmac-secret-mc" with salt1. Send a valid CTAP2 GetAssertion(0x02) message, with extensions that contain a valid "hmac-secret" extension request with salt1. Check that returned hmac secret equals to the hamc secret from MakeCredential.
P-3 Send a valid CTAP2 authenticatorMakeCredential(0x01) message with "hmac-secret-mc" with salt1 and salt2. Send a valid CTAP2 GetAssertion(0x02) message, with extensions that contain a valid "hmac-secret" extension request with salt1 and salt2. Check that returned hmac secrets are equal to the hamc secrets from MakeCredential.
F-1 Send a CTAP2 authenticatorMakeCredential(0x01) message, without necessary "hmac-secret" in extensions Check that Authenticator returns CTAP2_ERR_MISSING_PARAMETER(0x14) error code
F-2 Send a valid CTAP2 authenticatorMakeCredential(0x01) message, "extensions" containing "hmac-secret-mc" set to a random types, wait for the response, and check that Authenticator returns an error
F-3 Send a CTAP2 getAssertion(0x02) message, with "extensions" containing "hmac-secret-mc" extension request with one salt that is shorter than 32 bytes, wait for the response, and check that authenticator returns an error
F-4 Send a CTAP2 getAssertion(0x02) message, with "extensions" containing a "hmac-secret-mc" extension request with two salts, with second salt that is shorter than 32 bytes, wait for the response, and check that authenticator returns an error
CredProtect Test CredProtect extension support
P-1 Create a new (discoverable if supported) credential, with "extensions" containing a valid "credProtect" extension set to userVerificationOptional(0x01), and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. Send a valid GetAssertion(0x02) request with previously recorded credId and UV/UP set to false, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. If rk is supported, send a valid GetAssertion(0x02) request in RK mode(no credentialId), and UV/UP set to false, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. 4386ms‣
P-2 Create a new (discoverable if supported) credential, with "extensions" containing valid "credProtect" extension set to userVerificationOptionalWithCredentialIDList(0x02), and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. Send a valid GetAssertion(0x02) request with previously recorded credId and UV/UP set to false, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. Send a valid GetAssertion(0x02) request in RK mode(no credentialId), and UV/UP set to false, and check that Authenticator returns an error 4607ms‣
P-3 Create a new (discoverable if supported) credential, with "extensions" containing valid "credProtect" extension set to userVerificationRequired(0x03), and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. Send a valid GetAssertion(0x02) request with previously recorded credId and UV/UP set to false, and check that Authenticator returns an error Send a valid GetAssertion(0x02) request in RK mode(no credentialId), and UV/UP set to false, and check that Authenticator returns an error 4797ms‣
P-4 If rk and CredentialManagement is supported: (a) Create a new discoverable credential, with "extensions" containing valid "credProtect" extension set to a random level. (b) Call CredentialManagementAPI, find the corresponding credential, and check that credProtect level matches the set value. 5229ms‣
CredBlob Test CredBlob extension support
P-1 Check that GetInfo contains maxCredBlobLength(0x0F) field, and it is at least 32
P-2 Create a new discoverable credential, with "extensions" containing valid "credBlob" extension set to a random buffer, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. Send GetAssertion request with credBlob extension set to true, and check that result contains credBlob extension with expected bytes.
P-3 Create a new discoverable credential and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. Send GetAssertion request with credBlob extension set to true, and check that result contains credBlob extension with empty BYTE STRING
LargeBlob Test Large Blob extension support
P-1 Create a new credential using a makeCredential command with LargeBlob "support" set to "preferred". Validate that response unsignedExtensionOutputs (0x06) map contains a largeBlob key with "supported" set to true.
P-2 Create a new credential using a makeCredential command with LargeBlob "support" set to "required". Validate that response unsignedExtensionOutputs (0x06) map contains a largeBlob key with "supported" set to true.
P-3 Send a getAssertion request for the previously registered credential with LargeBlob extension: (a) "write" set to a byte string. (b) "originalSize" set to an unsigned integer. Validate that response unsignedExtensionOutputs (0x06) map contains a largeBlob key with "written" set to true.
P-4 Send a getAssertion request for the previously registered credential with LargeBlob extension "read" set to true. Validate that response unsignedExtensionOutputs (0x06) map contains a largeBlob key with: (a) "blob" with a value of type byte string. (b) "originalSize" with a value of type unsigned integer. Check that returned values match the values written in the previous test.
P-5 Send a getAssertion request for the previously registered RP without providing an allowList, with LargeBlob extension: (a) "write" set to a byte string. (b) "originalSize" set to an unsigned integer. Validate that response unsignedExtensionOutputs (0x06) map contains a largeBlob key with "written" set to false.
P-6 If authenticator DOES NOT create credentials with LargeBlob support enabled by default: Create a new credential without specifying LargeBlob extension. Send a getAssertion request for the previously registered credential, with LargeBlob extension: (a) "write" set to a byte string. (b) "originalSize" set to an unsigned integer. Validate that response unsignedExtensionOutputs (0x06) map contains a largeBlob key with "written" set to false.
P-7 If authenticator ALWAYS creates credentials with LargeBlob support enabled by default: Create a new credential without specifying LargeBlob extension. Send a getAssertion request for the previously registered credential, with LargeBlob extension: (a) "write" set to a byte string. (b) "originalSize" set to an unsigned integer. Validate that response unsignedExtensionOutputs (0x06) map contains a largeBlob key with "written" set to true.
F-1 Create a new credential with extensions containing largeBlob set to invalid CBOR map, and check that authenticator returns CTAP2_ERR_INVALID_CBOR(0x12)
F-2 Create a new credential using a makeCredential command. Set the extensions parameter to include a largeBlob entry. Send GetAssertion request with extensions containing largeBlob set to invalid CBOR map, and check that authenticator returns CTAP2_ERR_INVALID_CBOR(0x12)
F-3 Send GetAssertion request with extensions containing largeBlob set to invalid CBOR map (containing read, write and originalSize and the same time), and check that authenticator returns CTAP2_ERR_INVALID_CBOR(0x12)
F-4 Send GetAssertion request with extensions containing largeBlob set to invalid CBOR map with "read" set to invalid type, and check that authenticator returns CTAP2_ERR_INVALID_CBOR(0x12)
F-5 Send GetAssertion request with extensions containing largeBlob set to invalid CBOR map with "write" and "originalSize" set to invalid types, and check that authenticator returns CTAP2_ERR_INVALID_CBOR(0x12)
LargeBlobKey Test Large Blob Key extension support
P-1 Create a new credential with extensions containing largeBlobKey set to True, and check that authenticator succeeds, and that MakeCredential response contains largeBlobKey(0x05) key set to a random 32 byte BYTE STRING. Send a corresponding new MakeCredential request and see that largeBlobKeys do not match!
P-2 Send GetAssertion request for the previously registered credential with largeBlobKey extension set to true, and check that returned largeBlob key set to the previously recorded largeBlob key
F-1 Send MakeCredential request with largeBlobKey extension set to FALSE, and check that authenticator returns CTAP2_ERR_INVALID_OPTION(0x2C)
F-2 Send MakeCredential request with largeBlobKey extension set to not BOOLEAN, and check that authenticator returns CTAP2_ERR_CBOR_UNEXPECTED_TYPE(0x2C)
F-3 Send GetAssertion request for the previously registered credential with largeBlobKey extension set to FALSE, and check that authenticator returns CTAP2_ERR_INVALID_OPTION(0x2C)
F-4 Send GetAssertion request for the previously registered credential with largeBlobKey extension set to random type, and check that authenticator returns CTAP2_ERR_CBOR_UNEXPECTED_TYPE(0x2C)
MinPinLength Extension Test MinPinLength extension support
P-1 Create a new credential with extensions containing minPinLength set to True, and check that authenticator succeeds Check that MakeCredential response extensions contain minPinLength extension. If authenticator supports GetInfo minPINLength, check that minPinLength extension result equal to GetInfo.minPINLength
F-1 Create a new credential with extensions containing minPinLength set to True, and with the rpId not present in the authenticator’s minPinLengthRPIDs list. Check that Authenticator ignores the extension and does not return any authenticator extension output.
PinComplexityPolicy Extension Test PinComplexityPolicy extension support
P-1 Create a new credential with extensions containing pinComplexityPolicy set to True, and check that authenticator succeeds Check that MakeCredential response extensions contain pinComplexityPolicy extension, and check that it is of type boolean and set to true
P-2 Create a new credential with extensions containing pinComplexityPolicy set to True, and with the rpId not present in the authenticator’s minPinLengthRPIDs list. Check that Authenticator ignores the extension and does not return any authenticator extension output.
ThirdPartyPayment Test Third-Party Payment authentication extension support
P-1 Create a new discoverable credential, with "extensions" containing valid "thirdPartyPayment" extension set to true. Send GetAssertion request with thirdPartyPayment extension set to true, and check that result contains thirdPartyPayment extension with type boolean and set to true.
P-2 Create a new non-discoverable credential, with "extensions" containing valid "thirdPartyPayment" extension set to true. Send GetAssertion request with thirdPartyPayment extension set to true, and check that result contains thirdPartyPayment extension with type boolean and set to true.
F-1 Create a new non-discoverable credential, without any extensions. Send GetAssertion request with thirdPartyPayment extension set to true, and check that result contains thirdPartyPayment extension with type boolean and set to FALSE.
UVM Test User Verification Extension extension support
P-1 Create a new credential with extensions containing uvm extension input set to True, and check that authenticator succeeds Check that MakeCredential response extensions contain uvm extension output.
Authr-ClientPin1-GetKeyAgreement Test authenticatorClientPin(0x06), of version 0x01 support of getKeyAgreement(0x02) command
P-1 Send a valid CTAP2 authenticatorClientPin(0x06) message with getKeyAgreement(0x02) subCommand, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code, and: (a) check that authenticatorClientPin_Response contains "keyAgreement" field, and its of type MAP (b) in COSE "keyAgreement" field: (1) check that public key is EC2(kty(1) is set to 2) (2) check that key crv(-1) curve field that is set to P256(1) (3) check that key alg(3) is set to ECDH-ES+HKDF-256(-25) (4) check that key contains x(-2) is of type BYTE STRING, and is 32bytes long (5) check that key contains y(-3) is of type BYTE STRING, and is 32bytes long (6) check that key does NOT contains ANY other coefficients 981ms‣
Authr-ClientPin1-NewPin Test authenticatorClientPin(0x06), of version 0x01 support of setPIN(0x03), changePIN(0x04) and clientpin1_ObtainPINToken(0x05) commands
P-1 Generate a shared key by deriving sharedSecret from previously obtained keyAgreement, and set new random clientPin 1947ms‣
P-2 Change current pincode to the new pincode 2011ms‣
P-3 Get a valid pinAuth token 1930ms‣
P-4 Send a valid CTAP2 authenticatorMakeCredential(0x01) message with pinAuth, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. Check that authData.flags have UV flag set 4052ms‣
P-5 Send a valid CTAP2 authenticatorGetAssertion(0x02) message with pinAuth, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. Check that authData.flags have UV flag set 3571ms‣
P-6 Set forcePINChange to true, then change current pincode to the new pincode, check that Authenticator sets the value of the forcePINChange to false.
F-1 Set forcePINChange to true, send getPinToken which should return err until a successful PIN Change.
Authr-ClientPin1-Policy Check authenticator correctly implementing PinProtocol security policies
P-1 Try setting new pin, that is of size between minPINLength+1 and maxPINLength characters, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) 1972ms‣
F-1 Try setting new pin, that is less than 4 bytes, and check that Authenticator returns error CTAP2_ERR_PIN_POLICY_VIOLATION(0x37) 3485ms‣
F-2 Try setting new pin, that is bigger than 63 bytes, and check that Authenticator returns error CTAP2_ERR_PIN_POLICY_VIOLATION(0x37) 1902ms‣
F-3 Try setting new pin, that is exactly 64 bytes, and check that Authenticator returns error CTAP2_ERR_PIN_POLICY_VIOLATION(0x37) 1916ms‣
F-4 If authenticator supports maxPINLength (0x1D) Try setting new pin, that is of size maxPINLength+1, and check that Authenticator returns error CTAP2_ERR_PIN_POLICY_VIOLATION(0x37)
Authr-ClientPin1-GetRetries Test authenticatorClientPin(0x06), of version 0x01 support of getRetries(0x01) command
P-1 Send a valid CTAP2 authenticatorClientPin(0x06) message with getRetries(0x01) subCommand, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code, and: (a) check that authenticatorClientPin_Response contains "pinRetries" field (b) authenticatorClientPin_Response.retries is of type NUMBER (c) authenticatorClientPin_Response.retries is 8 or less! 844ms‣
P-2 Send two CTAP2 authenticatorClientPin(0x06) messages with getPinToken(0x05) subCommand, that contain invalid pinCode, and check that each request fails with error CTAP2_ERR_PIN_INVALID(0x31). Send a valid CTAP2 authenticatorClientPin(0x06) message with getRetries(0x01) subCommand, and check that retries have decreased by two. Register a valid authenticatorMakeCred(0x01) using the valid PIN. Check that retries counter is reset and back to the original retries counter. 9627ms‣
P-3 Send three CTAP2 authenticatorClientPin(0x06) messages with getPinToken(0x01) subCommand, that contain invalid pinCode, and check and check that for the last one authenticator returns CTAP2_ERR_PIN_AUTH_BLOCKED(0x34). 5871ms‣
P-4 Keep sending getPinToken subCommand with invalid pin until retries counter reaches 0. Send CTAP2 authenticatorClientPin(0x06) message with getPinToken(0x01) subCommand, that contains valid pinCode, and check that authenticator returns error CTAP2_ERR_PIN_BLOCKED(0x32) if (originalRetriesCounter <= 3)
Authr-ClientPin2-GetKeyAgreement Test authenticatorClientPin(0x06), of version 0x02 support of getKeyAgreement(0x02) command
P-1 Send a valid CTAP2 authenticatorClientPin(0x06) message with getKeyAgreement(0x02) subCommand, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code, and: (a) check that authenticatorClientPin_Response contains "keyAgreement" field, and its of type MAP (b) in COSE "keyAgreement" field: (1) check that public key is EC2(kty(1) is set to 2) (2) check that key crv(-1) curve field that is set to P256(1) (3) check that key alg(3) is set to ECDH-ES+HKDF-256(-25) (4) check that key contains x(-2) is of type BYTE STRING, and is 32bytes long (5) check that key contains y(-3) is of type BYTE STRING, and is 32bytes long (6) check that key does NOT contains ANY other coefficients 971ms‣
Authr-ClientPin2-NewPin Test authenticatorClientPin(0x06), of version 0x02 support of setPIN(0x03), changePIN(0x04) and getPinToken(0x05) commands
P-1 Generate a shared key by deriving sharedSecret from previously obtained keyAgreement, and set new random clientPin 1993ms‣
P-2 Change current pincode to the new pincode 2013ms‣
P-3 Set forcePINChange to true, then change current pincode to the new pincode, check that Authenticator sets the value of the forcePINChange to false.
P-4 Set pcmr(0x40) permission to true, then perform pincode change, check that Authenticator clears all persistent permissions.
Authr-ClientPin2-GetPinToken Test authenticatorClientPin(0x06), of version 0x02 support of getPinToken(0x05) commands
P-1 Get a valid pinAuth token 1981ms‣
P-2 Send a valid CTAP2 authenticatorMakeCredential(0x01) message with pinAuth, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. Check that authData.flags have UV flag set 4109ms‣
P-3 Send a valid CTAP2 authenticatorGetAssertion(0x02) message with pinAuth, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. Check that authData.flags have UV flag set 3543ms‣
F-1 Send a CredMan command with pinAuth that does not have permission, wait for the response, and check that Authenticator returns CTAP2_ERR_PIN_AUTH_INVALID(0x33) error code. 2823ms‣
F-2 Send a BiometricEnrollment command with pinAuth that does not have permission, wait for the response, and check that Authenticator returns CTAP2_ERR_PIN_AUTH_INVALID(0x33) error code.
F-3 Send a LargeBlob command with pinAuth that does not have permission, wait for the response, and check that Authenticator returns CTAP2_ERR_PIN_AUTH_INVALID(0x33) error code.
F-4 Send a Authenticator Config command with pinAuth that does not have permission, wait for the response, and check that Authenticator returns CTAP2_ERR_PIN_AUTH_INVALID(0x33) error code.
F-5 Set forcePINChange to true, send getPinToken which should return err until a successful PIN Change.
Authr-ClientPin2-GetPinUvAuthTokenUsingPinWithPermissions Test authenticatorClientPin(0x06), of version 0x02 support of getPinUvAuthTokenUsingPinWithPermissions(0x09) commands
P-1 Get a valid pinUVAuthToken with permissions 1971ms‣
P-2 Send a valid CTAP2 authenticatorMakeCredential(0x01) message creating discoverable credential with pinAuth, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. Check that authData.flags have UV flag set 4141ms‣
P-3 Send a valid CTAP2 authenticatorGetAssertion(0x02) message with pinAuth, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code. Check that authData.flags have UV flag set 3499ms‣
P-4 Send a valid CTAP2 getPinUvAuthTokenUsingPinWithPermissions(0x09) message with pcmr(0x40) permission and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code.
F-1 Set forcePINChange to true, send getPinUvAuthTokenUsingPinWithPermissions which should return err until a successful PIN Change.
Authr-ClientPin2-Policy Check authenticator correctly implementing PinProtocol security policies
P-1 Try setting new pin, that is of size between minPINLength+1 and maxPINLength characters, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) 1963ms‣
F-1 Try setting new pin, that is less than 4 bytes, and check that Authenticator returns error CTAP2_ERR_PIN_POLICY_VIOLATION(0x37) 3452ms‣
F-2 Try setting new pin, that is bigger than 63 bytes, and check that Authenticator returns an error 1906ms‣
F-3 Try setting new pin, that is exactly 64 bytes, and check that Authenticator returns error CTAP2_ERR_PIN_POLICY_VIOLATION(0x37) 1927ms‣
F-4 If authenticator supports maxPINLength (0x1D) Try setting new pin, that is of size maxPINLength+1, and check that Authenticator returns error CTAP2_ERR_PIN_POLICY_VIOLATION(0x37)
F-5 Try setting new pin, that consists of Unicode codepoints of size minPINLength - 1, and check that Authenticator returns error CTAP2_ERR_PIN_POLICY_VIOLATION(0x37) 1916ms‣
Authr-ClientPin2-GetRetries Test authenticatorClientPin(0x06), of version 0x02 support of getPINRetries(0x01) command
P-1 Send a valid CTAP2 authenticatorClientPin(0x06) message with getRetries(0x01) subCommand, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code, and: (a) check that authenticatorClientPin_Response contains "retries" field (b) authenticatorClientPin_Response.retries is of type NUMBER (c) authenticatorClientPin_Response.retries is 8 or less! 840ms‣
P-2 Send a valid CTAP2 authenticatorClientPin(0x06) message with getUVRetries(0x07) subCommand, wait for the response, and check that Authenticator returns CTAP1_ERR_SUCCESS(0x00) error code, and: (a) check that authenticatorClientPin_Response contains "uvRetries(0x05)" field (b) authenticatorClientPin_Response.retries is of type NUMBER (c) authenticatorClientPin_Response.retries is 1 or more! (d) authenticatorClientPin_Response.retries is 25 or less!
P-3 Send two CTAP2 authenticatorClientPin(0x06) messages with getPinToken(0x01) subCommand, that contain invalid pinCode, and check that each request fails with error CTAP2_ERR_PIN_INVALID(0x31). Send a valid CTAP2 authenticatorClientPin(0x06) message with getRetries(0x01) subCommand, and check that retries have decreased by two. Register a valid authenticatorMakeCred(0x01) using the valid PIN. Check that retries counter is reset and back to the original retries counter. 9593ms‣
P-4 Send three CTAP2 authenticatorClientPin(0x06) messages with getPinToken(0x01) subCommand, that contain invalid pinCode, and check and check that for the last one authenticator returns CTAP2_ERR_PIN_AUTH_BLOCKED(0x34). 5854ms‣
P-5 Keep sending getPinToken subCommand with invalid pin until retries counter reaches 0. Send CTAP2 authenticatorClientPin(0x06) message with getPinToken(0x01) subCommand, that contains valid pinCode, and check that authenticator returns error CTAP2_ERR_PIN_BLOCKED(0x32) 20778ms‣
Authr-CredentialManagement-EnumerateRPs Test authenticatorCredentialManagement(0x0A) command support for discoverable credential metadata and enumeration functionality for RPs
P-1 If authenticator supports Credential Management API: Create new discoverable credential. Send authenticatorCredentialManagement(0x0A) with getCredsMetadata(0x01), and check that existingResidentCredentialsCount is 1, and maxPossibleRemainingResidentCredentialsCount is more than 1. Create another discoverable credential, and make sure that existingResidentCredentialsCount is now 2. 13847ms‣
P-2 If authenticator supports Credential Management API: Send authenticatorCredentialManagement(0x0A) with enumerateRPsBegin(0x02), and check that result: (a) Result.rp is present and of type MAP (b) Result.rp.id is present and is of type String. (c) Result.rp.id is in a list of known rpIDs. (d) Result.rpIDHash is a valid SHA-256 hash of Result.rp.id, and is of type BYTESTRING. (e) Result.totalRPs is a Number and is set to 2, same as a number of registered RPIDs. 2828ms‣
P-3 If authenticator supports Credential Management API: Send authenticatorCredentialManagement(0x0A) with enumerateRPsGetNextRP(0x03), and check that result: (a) Result.rp is present and of type MAP (b) Result.rp.id is present and is of type String. (c) Result.rp.id is in a list of known rpIDs. (d) Result.rpIDHash is a valid SHA-256 hash of Result.rp.id, and is of type BYTESTRING. 465ms‣
P-4 If authenticator supports Credential Management API: Create new discoverable credential, with pcmr permission of PUAT. Send authenticatorCredentialManagement(0x0A) with getCredsMetadata(0x01) and check that authenticator returns excepted authenticatorCredentialManagement response.
P-5 If authenticator supports Credential Management API: Create new discoverable credential, with pcmr permission of PUAT. Send authenticatorCredentialManagement(0x0A) with enumerateRPsBegin(0x02), and check that authenticator returns excepted authenticatorCredentialManagement response.
P-6 If authenticator supports Credential Management API: Create new discoverable credential, with pcmr permission of PUAT. Send authenticatorCredentialManagement(0x0A) with enumerateRPsGetNextRP(0x03), and check that authenticator returns excepted authenticatorCredentialManagement response.
Authr-CredentialManagement-EnumerateCredentials Test authenticatorCredentialManagement(0x0A) command support for discoverable credential enumeration
P-1 If authenticator supports Credential Management API: Create two new discoverable credentials. Send authenticatorCredentialManagement(0x0A) with enumerateCredentialsBegin(0x04), and make sure that: (a) Response.credentialID is of type PublicKeyCredentialDescriptor, and matching previously recorded credentialID (b) Response.user is of type PublicKeyCredentialUserEntity, and at least contains id, that matches previously recorded (c) Response.publicKey is a COSE_Key and matches previously recorded (d) Response.totalCredentials is a number and set to 2, same as registered credentials (e) Response.credProtect is a number and level 1, 2 or 3 (f) Response.largeBlobKey is of type BYTE STRING (g) Response.thirdPartyPayment is a boolean 3152ms‣
P-2 If authenticator supports Credential Management API: Create two new discoverable credentials. Send authenticatorCredentialManagement(0x0A) with enumerateCredentialsGetNextCredential(0x05), and make sure that: (a) Response.credentialID is of type PublicKeyCredentialDescriptor, and matching previously recorded credentialID (b) Response.user is of type PublicKeyCredentialUserEntity, and at least contains id, that matches previously recorded (c) Response.publicKey is a COSE_Key and matches previously recorded (d) Response.totalCredentials is undefined (e) Response.credProtect is a number and level 1, 2 or 3 (f) Response.largeBlobKey is of type BYTE STRING (g) Response.thirdPartyPayment is a boolean 632ms‣
P-3 If authenticator supports Credential Management API: Create new discoverable credential, with pcmr permission of PUAT. Send authenticatorCredentialManagement(0x0A) with enumerateCredentialsBegin(0x04) and check that authenticator returns excepted authenticatorCredentialManagement response.
Authr-CredentialManagement-UpdateAndDelete Test authenticatorCredentialManagement(0x0A) command, for support of Update and Delete functionality for discoverable credentials
P-1 If authenticator supports Credential Management API: Create a discoverable credential. Send authenticatorCredentialManagement(0x0A) with updateUserInformation(0x07), and make sure that: (a) At least Response.user.id is present and matches new value. (b) If authenticator supports name and displayName fields, make sure that updated value is correct and missing field is now removed. (с) If authenticator supports encCredStoreState, check that it generates new random value for the credential store state (ct). Send getInfo request and check that it regenerates new encryption iv for the credential store state. 6882ms‣
P-2 If authenticator supports Credential Management API: Send authenticatorCredentialManagement(0x0A) with deleteCredential(0x06), and check that authenticator succeeds. Send subsequent GetAssertion request with deleted credential credID specified in allow list and check that authenticator returns CTAP2_ERR_NO_CREDENTIALS(0x2E). If authenticator supports encCredStoreState, check that it generates new random value for the credential store state (ct). Send getInfo request and check that it regenerates new encryption iv for the credential store state. 7091ms‣
Metadata-Stmt-1 Verify compliance of Metadata statement fields.
P-1 If Metadata contains "legalHeader", it MUST be of type DOMString, and MUST not be empty‣
P-2 Required "aaguid", of type DOMString, not empty, and encoded as specified in RFC4122(i.e: f81d4fae-7dec-11d0-a765-00a0c91e6bf6)‣
P-3 Required Metadata.aaid, Metadata.attestationCertificateKeyIdentifiers and Metadata.supportedExtensions fields to be undefined.‣
P-4 Required Metadata.description, of type DOMString.‣
P-5 If Metadata contain "alternativeDescriptions" field, then: (a) Check that its of type Dictionary (b) Check that each of the keys is correctly formated IETF language code(ru, fr-FR) (c) Check that values are of type DOMString, and are not empty (d) Check that it's max length of 200 characters
P-6 Required Metadata.authenticatorVersion, of type unsigned short.‣
P-7 Required Metadata.protocolFamily field, it must be of type DOMString, and equal to "fido2".‣
P-8 Required "upv", of type Version[]. MUST not be empty. MUST contain { "major": 1, "minor": 3 } aks CTAP2.3.‣
P-11 Required Metadata.authenticationAlgorithms field, of type SEQUENCE. Each member of the SEQUENCE must be of type DOMString, and a valid short form authentication ALG authenticationAlgorithms (e.g. "secp256r1_ecdsa_sha256_raw") in the Registry of Predefined Values.‣
P-13 Required Metadata.publicKeyAlgAndEncodings field, of type SEQUENCE. Each member of the SEQUENCE must be of type DOMString, and a valid short form public key encoding ALG identifier(e.g. "ecc_x962_raw") in the Registry of Predefined Values. For FIDO2 MUST include "cose"‣
P-14 Required Metadata to contain "attestationTypes" field, of type SEQUENCE. It MUST NOT be empty. Each member MUST be of type String, and MUST be a valid DOMString ATTESTATION short form(e.g. "basic_full") constants in FIDO Registry.‣
P-15 If Metadata contains "userVerificationDetails" field, of type SEQUENCE. It MUST NOT be empty. For each VerificationMethodDescriptor in the two dimension SEQUENCE check that: (a) "userVerificationMethod" field is NOT missing, and its value is set to a single USER_VERIFY short constant(e.g. "passcode_internal") in FIDO Registry (b) If "caDesc" is presented: (1) Check that "userVerificationMethod" field is set to "passcode_internal" or "passcode_external". (2) Check that its of type Dictionary (3) Check that "caDesc" contains "base" field, and its of type Number, and is not 0 or less (4) Check that "caDesc" contains "minLength" field, and its of type Number, and is not 0 or less (5) If "caDesc" contains "maxRetries" field, and its of type Number, and not less than 0 (6) If "caDesc" contains "blockSlowdown" field, and its of type Number, and not less than 0 (c) If "baDesc" is presented: (1) Check that "userVerificationMethod" field is set to one of "fingerprint_internal", "voiceprint_internal", "faceprint_internal", "eyeprint_internal", or "handprint_internal". (2) Check that its of type Dictionary (3) If "baDesc" contains "selfAttestedFRR" field, and its of type Number, and is not 0 or less, and less than or equal to 1 (with 1 representing 100%) (4) If "baDesc" contains "selfAttestedFAR" field, and its of type Number, and is not 0 or less, and less than or equal to 1 (with 1 representing 100%) (5) If "baDesc" contains "iAPARThreshold" field, and its of type Number, and is not 0 or less, and less than or equal to 1 (with 1 representing 100%) (6) If "baDesc" contains "maxTemplates" field, and its of type Number, and is not 0 or less (7) If "baDesc" contains "maxRetries" field, and its of type Number, and not less than 0 (8) If "baDesc" contains "blockSlowdown" field, and its of type Number, and not less than 0 (d) If "paDesc" is presented: (1) Check that "userVerificationMethod" field is set to "pattern_internal" or "pattern_external". (2) Check that its of type Dictionary (3) Check that "paDesc" contains "minComplexity" field, and its of type Number, and is not 0 or less (4) If "paDesc" contains "maxRetries" field, and its of type Number, and not less than 0 (5) If "paDesc" contains "blockSlowdown" field, and its of type Number, and not less than 0 (e) "userVerificationMethod" MUST not be set "all"!‣
P-16 Required Metadata to contain "keyProtection" field, that is a SEQUENCE of DOMString KEY_PROTECTION short form constants(e.g. "secure_element") MUST not be empty "software" can not be combined with "hardware" "software" can not be combined with "tee" "software" can not be combined with "secure_element" "tee" should be combined with "hardware" "secure_element" should be combined with "hardware"‣
P-17 If "isKeyRestricted" is presented, it must be of type boolean.
P-18 If "isFreshUserVerificationRequired" is presented, it must be of type boolean.
P-19 Required Metadata to contain "matcherProtection" field, that is a SEQUENCE of DOMString MATCHER_PROTECTION short form constants(e.g. "on_chip") MUST not be empty If multiple userVerificationMethods are present, it MUST contain one entry per method or the minimum security level across all methods.‣
P-20 If Metadata contains "cryptoStrength" field, check that its of type number, and is larger than 0.‣
P-22 Required Metadata to contain "attachmentHint", that is a SEQUENCE of DOMString ATTACHMENT_HINT short form constants(e.g. "external") "internal" CAN NOT be combined with any other flags "external" MUST be combined with one or more flags "ready" MUST NOT be set "usb", "nfc", "ble", "internal" "smart-card" MUST be combined with "wired"‣
P-24 Required Metadata to contain "tcDisplay" field, that is a SEQUENCE of DOMString TRANSACTION_CONFIRMATION_DISPLAY short form constants(e.g. "privileged_software") "privileged_software" MUST not be combined with "tee" or/and "hardware" "hardware" MUST not be combined with "tee" or/and "privileged_software" "tee" MUST not be combined with "privileged_software" or/and "hardware" If authenticatorGetinfo.extensions contain "txAuthSimple" or "txAuthGeneric", tcDisplay MUST not be empty Otherwise tcDisplay MUST be empty‣
P-25 If extensions contain "txAuthSimple", tcDisplayContentType MUST be set to "text/plain" If extensions contain "txAuthGeneric", tcDisplayContentType MUST be set to "image/png" Otherwise tcDisplayContentType MUST undefined‣
P-26 If tcDisplay is not 0, and tcDisplayContentType is set to "image/png", check that tcDisplayPNGCharacteristics, of type DisplayPNGCharacteristicsDescriptor[], and not empty‣
P-27 Required attestationRootCertificates, of type DOMString[]. MUST be present and non-empty if attestationTypes contains other values than "basic_surrogate". Each entry must be a Base64-encoded (section 4 of [[RFC4648]]), DER-encoded [[ITU-X690-2008]] PKIX certificate value.‣
P-28 If "ecdaaTrustAnchors" field presented, it must be of type SEQUENCE, and for each EcdaaTrustAnchor entry check that: "EcdaaTrustAnchor.X" is of type DOMString, and its NOT empty. "EcdaaTrustAnchor.Y" is of type DOMString, and its NOT empty. "EcdaaTrustAnchor.c" is of type DOMString, and its NOT empty. "EcdaaTrustAnchor.sx" is of type DOMString, and its NOT empty. "EcdaaTrustAnchor.sy" is of type DOMString, and its NOT empty. "EcdaaTrustAnchor.G1Curve" is of type DOMString, and its NOT empty.
P-29 If Metadata contains "icon" field, it MUST be of type DOMString, not empty, set to URL [RFC2397] encoded PNG or SVG11‣
P-31 Required Metadata.schema to be a number, value of 3.‣
P-32 REQUIRED: authenticatorGetInfo, a MAP that contains such keys: (a) Required "versions": a SEQUENCE of DOMStrings that identifying supported protocol versions. For CTAP2.0(UPV 1.0) must contain: FIDO_2_0. For For CTAP2.1(UPV 1.1) must contain: FIDO_2_1. MUST NOT contain duplicate values nor be empty if present. (b) Required "aaguid": a DOMString hex encoded aaguid byte string. MUST match Metadata.aaguid (c) If "extensions" present: MUST be a DOMString SEQUENCE of extension identifiers. (c) If "options" present: MUST be a MAP of <DOMString/Boolean> which represents support for authenticator options (c) If "pinUvAuthProtocols" present: MUST a SEQUENCE of Numbers defining PIN protocols support. MUST NOT contain duplicate values nor be empty if present. (c) If "maxMsgSize" present: MUST be a positive integer. MUST not be 0 (c) If "maxCredentialCountInList" present: MUST be of type Number(unsigned int) and above 0 (c) If "maxCredentialIdLength" present: MUST be of type Number(unsigned int) and above 0 (c) If "transports" present: A DOMString SEQUENCE of supported transports. Defined in WebAuthn#AuthenticatorTransport enum. MUST NOT include duplicate values nor be empty if present. (c) If "algorithms" present: A SEQUENCE of PublicKeyCredentialParameters MAP. Must match Metadata.authenticationAlgorithms. MUST NOT include duplicate entries nor be empty if present. (c) If "maxSerializedLargeBlobArray" present: MUST be of type Number(unsigned int) and >= 1024 (c) If "forcePINChange" present: MUST be Boolean. If "pinUvAuthProtocols" is missing, MUST NOT be present. (c) If "minPINLength" present: MUST be positive integer. MUST NOT be 0. If "pinUvAuthProtocols" is missing, MUST NOT be present. (c) If "firmwareVersion" present: MUST be a positive integer. MUST be equal to Metadata.authenticatorVersion. (c) If "maxCredBlobLength" present: MUST be positive integer. MUST NOT be 0. MUST be at least 32. (c) If "maxRPIDsForSetMinPINLength" present: maxRPIDsForSetMinPINLength (c) If "preferredPlatformUvAttempts" present: MUST be positive integer. MUST NOT be 0. (c) If "uvModality" present: This specifies the user verification modality supported by the authenticaton. (c) If "certifications" present: This specifies a list of authenticator certifications. Certificates MUST be within their provided ranges. All are between 1 and 4-7. (c) If "remainingDiscoverableCredentials" present: MUST be an unsigned integer. MUST NOT be below 0. (c) If "vendorPrototypeConfigCommands" present: Authenticator supports the authenticatorConfig vendorPrototype subcommand, and its value is a list of authenticatorConfig vendorCommandId values supported, which MAY be empty.‣
P-33 Check that deprecated fields are not present‣
P-34 Check that only allowed fields, defined in the Metadata Statement specification is presented in the metadata. The list of allowed fields is: legalHeader, aaid, aaguid, attestationCertificateKeyIdentifiers, friendlyNames, description, alternativeDescriptions, authenticatorVersion, protocolFamily, schema, upv, authenticationAlgorithms, publicKeyAlgAndEncodings, attestationTypes, userVerificationDetails, keyProtection, isKeyRestricted, isFreshUserVerificationRequired, matcherProtection, cryptoStrength, attachmentHint, tcDisplay, tcDisplayContentType, tcDisplayPNGCharacteristics, attestationRootCertificates, ecdaaTrustAnchors, icon, iconDark, providerLogoLight, providerLogoDark, supportedExtensions, multiDeviceCredentialSupport, authenticatorGetInfo, cxpConfigURL‣
P-35 Check that metadata fields are not reused from the FIDO2 metadata example‣
P-36 If Metadata contains "legalHeader", check that it's value is set to the expected value for MDS3‣
P-37 If Metadata contain "friendlyNames" field, then: (a) Check that its of type Dictionary (b) Check that each of the keys is correctly formated IETF language code(de, de-AT) (c) Check that values are of type DOMString, and are not empty (d) Check that it's max length of 63 characters (e) Check that it contains at least one English language entry
P-38 If Metadata contains "iconDark" field, it MUST be of type DOMString, not empty, set to URL [RFC2397] encoded SVG
P-39 If Metadata contains "providerLogoLight" field, it MUST be of type DOMString, not empty, set to URL [RFC2397] encoded SVG [SVG11], then: (a) Check that the format is SVG Version: 1.2 with baseProfile as “tiny-ps" (b) Check that the elements is vector-based (cannot contain raster components) (c) Check that the dimensions is square aspect ratio (d) Check that the title element populated with the English version of the provider friendly name (e) Check that the SVG does not contain comments or extra text
P-40 If Metadata contains "providerLogoDark" field, it MUST be of type DOMString, not empty, set to URL [RFC2397] encoded SVG [SVG11], then: (a) Check that the format is SVG Version: 1.2 with baseProfile as “tiny-ps" (b) Check that the elements is vector-based (cannot contain raster components) (c) Check that the dimensions is square aspect ratio (d) Check that the title element populated with the English version of the provider friendly name (e) Check that the SVG does not contain comments or extra text
P-41 If Metadata contains "keyScope" field, it MUST be of type DOMString, not empty and contain only valid values ("public-key-credential-source", "device-spk", or "provider-spk")
P-42 If Metadata contains "multiDeviceCredentialSupport" field, it MUST be of type DOMString, not empty and contain only valid values ("unsupported", "explicit", or "implicit")
P-43 If Metadata contains "cxpConfigURL" field, it MUST be of type DOMString, not empty